# 🔧 Исправления на основе анализа логов

## 📋 Приоритет исправлений

### Критично (немедленно):
1. ✅ Заменить экспериментальную модель на стабильную
2. ✅ Добавить ограничение AI запросов при RSS парсинге
3. ✅ Добавить обработку ошибок для цен и индекса страха

### Важно (в ближайшее время):
4. ⚠️ Исправить миграцию userbot (или пропустить)
5. ⚠️ Увеличить интервал Queue Poster
6. ⚠️ Добавить retry для 429 ошибок

### Желательно:
7. ℹ️ Улучшить cleanup при выходе
8. ℹ️ Подавить Pydantic warnings

---

## Готовые исправления кода

### Исправление 1: Стабильная модель Gemini

**Файл:** `services/ai_summary.py`

```python
def _find_best_model(self):
    """Выбирает стабильную модель для free tier"""
    # Используем стабильную модель вместо экспериментальной
    logger.info("✅ Используем стабильную модель: gemini-1.5-flash (free tier compatible)")
    return 'gemini-1.5-flash'
```

---

### Исправление 2: Ограничение AI запросов

**Файл:** `main.py`, функция `scheduled_parsing()`

Добавить ограничение на количество AI запросов за цикл.

---

### Исправление 3: Обработка ошибок для цен

**Файл:** `main.py`, функция `check_queue_and_post()`

Добавить try-except для `get_multiple_crypto_prices()` и `get_fear_greed_index()`.

---

### Исправление 4: Увеличить интервал Queue Poster

**Файл:** `main.py`

Изменить `IntervalTrigger(seconds=30)` на `IntervalTrigger(seconds=60)`.

---

### Исправление 5: Пропустить миграцию если не работает

**Файл:** `services/telegram_listener.py`

Упростить логику миграции - если не работает, просто пропустить.

---

*Документ создан для исправления найденных проблем*
