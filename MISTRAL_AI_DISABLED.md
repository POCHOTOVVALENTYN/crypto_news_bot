# ⚠️ Mistral AI временно отключен

## 🔍 Проблема

**Конфликты зависимостей с Mistral AI:**

1. **Конфликт pydantic:**
   - `mistralai>=1.3.0` требует `pydantic>=2.9.0`
   - `mistralai>=1.5.2` требует `pydantic>=2.10.3`
   - `aiogram 3.3.0` требует `pydantic<2.6,>=2.4.1`
   - ❌ **Несовместимо!**

2. **Конфликт httpx:**
   - `mistralai<1.3.0` требует `httpx>=0.27.0,<0.28.0`
   - `google-genai` требует `httpx>=0.28.1,<1.0.0`
   - ❌ **Несовместимо!**

---

## ✅ Решение

**Временно отключен Mistral AI** до решения конфликтов зависимостей.

### Изменения:

1. **requirements.txt:**
   - Закомментирован `mistralai>=1.0.0`
   - Добавлен комментарий с объяснением

2. **services/ai_summary.py:**
   - Отключена инициализация Mistral AI (`if False`)
   - Отключен fallback на Mistral AI
   - Обновлены сообщения об ошибках (fallback сразу на OpenAI)

---

## 🔄 Текущая архитектура fallback

```
Gemini (основной, free tier, 4.5 сек)
  ↓ (429 / ошибка)
OpenAI (fallback, платный, 1 сек)
```

**Изменения:**
- Убран Mistral AI из цепочки
- Fallback сразу переключается на OpenAI при ошибках Gemini

---

## 💡 Альтернативные решения (будущее)

### Вариант 1: Обновить aiogram

Обновить `aiogram` до версии, которая поддерживает `pydantic>=2.9.0`:
- Позволит использовать `mistralai>=1.3.0`
- Но потребует обновления всего кода под новую версию aiogram

### Вариант 2: Сделать Mistral опциональным

Установить Mistral AI отдельно (не через requirements.txt):
- Пользователь может установить вручную при необходимости
- Бот будет работать без Mistral AI

### Вариант 3: Использовать более старую версию Mistral

Найти версию `mistralai<1.3.0`, которая работает с:
- `pydantic<2.6`
- `httpx>=0.28.1` (маловероятно, что такая версия существует)

---

## 📋 Текущие зависимости

| Пакет | Требование pydantic | Требование httpx | Статус |
|-------|---------------------|------------------|--------|
| aiogram 3.3.0 | `<2.6,>=2.4.1` | - | ✅ Активен |
| google-genai | `>=2.0.0,<3.0.0` | `>=0.28.1,<1.0.0` | ✅ Активен |
| openai 1.40.0 | `>=1.9.0,<3` | `>=0.23.0,<1.0.0` | ✅ Активен |
| mistralai | `>=2.9.0` или `>=2.10.3` | `<0.28.0` или `>=0.28.1` | ❌ Отключен |

---

## 🚀 Следующие шаги

1. **Установить зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Бот будет работать с Gemini и OpenAI:**
   - Gemini как основной провайдер
   - OpenAI как fallback

3. **Когда потребуется Mistral AI:**
   - Обновить aiogram до версии, поддерживающей `pydantic>=2.9.0`
   - Или найти альтернативное решение

---

*Mistral AI отключен: $(date)*

